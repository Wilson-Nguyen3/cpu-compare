# ─── Example env for contributors (safe to commit) ──────────────────────────
# Copy this to .env and fill in your real values — never commit .env itself.

DB_HOST=172.25.64.1
DB_PORT=5433
DB_NAME=cpu_scores
DB_USER=postgres
DB_PASSWORD=YOUR_PASSWORD_HERE

NGINX_PORT=8082
